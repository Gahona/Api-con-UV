[project]
name = "api-con-uv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.0.0",
    "ollama>=0.6.2",
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
api-con-uv = "api_con_uv:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
